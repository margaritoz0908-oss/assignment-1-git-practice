# Git Practice Project

This simple website was created to practice the Git command-line workflow.

## Files

- `index.html` contains the website content.
- `styles.css` controls the website's appearance.

## Commands Practiced

```bash
git status
git add .
git commit -m "Describe the change"
git log --oneline
```
